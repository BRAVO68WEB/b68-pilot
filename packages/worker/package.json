{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bunx tsc",
    "dev": "bun run index.ts",
    "webhook": "bun run server.ts"
  },
  "dependencies": {
    "core": "workspace:*",
    "croner": "^7.0.2"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
